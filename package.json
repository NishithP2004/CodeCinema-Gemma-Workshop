{
  "name": "codec-cinema",
  "version": "0.1.0",
  "private": true,
  "description": "CodeCinema turns local code repositories into cited, narrated explainer videos.",
  "main": "src/server.js",
  "scripts": {
    "dev": "node src/server.js",
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/repoScanner.js && node --check src/ollamaClient.js && node --check src/explainer.js && node --check src/video.js && node --check src/git.js && node --check src/utils.js"
  },
  "engines": {
    "node": ">=18"
  }
}
